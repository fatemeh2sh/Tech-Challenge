package com.sharif.tech_challenge.utils

enum class CodeType(var type:Int) {
    Picture(0),
    Vibrate(1),
    Sound(2)
}

enum class ThemeType(var type:String) {
    Sport("sport"),
    Art("art"),
    Fun("fun")
}
